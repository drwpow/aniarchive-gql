# 👻 AniArchive GQL

GraphQL API for querying the Animation Archive.

## Setup

Copy `.env.example` to `.env` and fill in secrets. Then run:

```bash
npm i
```

## Local dev

```bash
npm run dev
```
